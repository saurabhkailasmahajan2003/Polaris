# Only need the backend URL — all other settings come from backend/.env via GET /api/config
VITE_BACKEND_URL=http://localhost:5000

# Optional fallback if backend is unreachable (normally set in backend/.env instead)
# VITE_CLERK_PUBLISHABLE_KEY=
